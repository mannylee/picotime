# Build output — assembled by build.sh, regenerated on every build
Picotime.app/

# Swift / swiftc intermediates
*.o
*.swiftmodule
.build/
DerivedData/

# Coverage reports (./coverage.sh --html <dir>; raw data lives under .build/)
/cov/
*.profraw
*.profdata

# macOS
.DS_Store
